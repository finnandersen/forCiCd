# Allure - Быстрый старт

## ✅ Что уже настроено

- ✅ Allure зависимости добавлены в `build.gradle`
- ✅ AspectJ настроен для поддержки аннотаций
- ✅ Примеры Allure аннотаций добавлены в тесты
- ✅ Gradle задачи для генерации отчетов созданы

## 🚀 Быстрый запуск

### 1. Запустить тесты
```bash
./gradlew test
```

Результаты сохранятся в `build/allure-results/`

### 2. Сгенерировать отчет
```bash
./gradlew allureReport
```

Отчет будет в `build/allure-report/index.html`

### 3. Открыть отчет в браузере
```bash
./gradlew allureServe
```

## 📋 Использованные Allure аннотации

В тестах уже добавлены примеры:

- `@Epic` - группировка тестов по функциональным областям
- `@Feature` - группировка по фичам
- `@Story` - пользовательские истории
- `@Description` - описание теста
- `@Severity` - уровень критичности (BLOCKER, CRITICAL, NORMAL, MINOR, TRIVIAL)

## 📝 Дополнительные аннотации (опционально)

Вы можете добавить в тесты:

```java
@Step("Выполнение запроса к API")
public void makeRequest() {
    // код
}

@Attachment(value = "Response", type = "application/json")
public String attachResponse(String response) {
    return response;
}
```

## 🔧 Требования

Установите Allure Commandline для генерации отчетов:

**Windows:**
```bash
scoop install allure
# или
choco install allure-commandline
```

**MacOS:**
```bash
brew install allure
```

**Linux:**
```bash
sudo apt-get install allure
```

## 📁 Структура

```
build/
├── allure-results/     # JSON результаты тестов
└── allure-report/      # HTML отчет
    └── index.html      # Откройте в браузере
```

## 🔗 Полезные ссылки

- [Документация Allure](https://allurereport.org/docs/)
- [Allure JUnit 5](https://allurereport.org/docs/junit5/)
